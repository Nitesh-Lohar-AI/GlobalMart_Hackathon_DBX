# The main pipeline for globalmart_hackathon_dbx_project

resources:
  pipelines:
    globalmart_hackathon_dbx_project_etl:
      name: globalmart_hackathon_dbx_project_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/globalmart_hackathon_dbx_project_etl"

      libraries:
        - glob:
            include: ../src/globalmart_hackathon_dbx_project_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
