# This is a Databricks asset bundle definition for globalmart_hackathon_dbx_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: globalmart_hackathon_dbx_project
  uuid: 764454f1-4c58-43fa-b0ad-4df529784bdd

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-f642ffc2-498c.cloud.databricks.com
    variables:
      catalog: dbx_hck_glbl_mart
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-f642ffc2-498c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/rahul.vaghasia@kenexai.com to make sure we only have a single copy.
      root_path: /Workspace/Users/rahul.vaghasia@kenexai.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbx_hck_glbl_mart
      schema: prod
    permissions:
      - user_name: rahul.vaghasia@kenexai.com
        level: CAN_MANAGE
      - user_name: nitesh.lohar@kenexai.com
        level: CAN_MANAGE
      - user_name: mukesh.goswami@kenexai.com
        level: CAN_MANAGE